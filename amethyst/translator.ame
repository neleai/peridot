amethyst Peridot_translator{
	root= trans*
	
	trans = Def[{@@names=["placeholder"]} trans:body {"obj #{@name}(#{@args*""}){obj #{@@names.uniq*","}; #{body};}" }]
				| Call[trans*:args  {"#{@name}(#{args*","})"} ]
				| CCode[ .]
				| If[ @expr=>trans:expr @block=>trans:block {"if ((#{expr}).in){#{block};}" }]
        | Var[                       {@@names<<@name}  @name ]
				| Assign[@expr=>trans:expr   {@@names<<@name}  {"#{@name}=#{expr}"}]
				| Seq[ trans*:{it*";"}]
				| .
}
