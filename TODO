persistent sockets

protocols, algebraic description...

functors to supports static analyzers like code enclosed by 
without_error(Type_error){block}            
without_error(Division_by_zero){block}
without_error(Side_Effect){block}
block does not contain type error/cant divide by zero...

define local variable by variable:=expr for sane scoping

binding object

errors etc to crossref files(crossref file syntax text <inserted text> more text

garbage collection bfs to fit pages

profiling regions user gives brownie point reward for speedup

create closure when lambda escape 
